<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <data>

    <import type="android.view.View" />

    <variable
        name="viewModel"
        type="net.yslibrary.historian.uncaught_handler.internal.ui.view_models.UncaughtViewModel" />
  </data>

  <RelativeLayout
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      tools:context=".internal.ui.activity.UncaughtActivity">

    <FrameLayout
        android:id="@+id/back"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:background="@color/historian_backgorund"
        android:background="@{viewModel.backgorundColor}">

      <ImageView
          android:id="@+id/image"
          android:layout_width="match_parent"
          android:layout_height="match_parent"
          tools:src="@drawable/historian_cow_error"
          android:src="@{viewModel.imagePath}" />

      <LinearLayout
          android:orientation="vertical"
          android:gravity="end"
          android:layout_gravity="bottom|end"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="@string/historian_crash_text"
            tools:textColor="@color/historian_crash_text"
            android:text="@{viewModel.crashText}"
            android:textColor="@{viewModel.crashTextColor}"
            android:textSize="18sp"
            android:textStyle="bold"
            android:id="@+id/textViewC" />

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

          <Button
              android:id="@+id/btnRestart"
              tools:text="@string/historian_restart_app_button_text"
              android:text="@{viewModel.restartAppButtonText}"
              android:visibility="@{viewModel.restartActivityEnable ? View.VISIBLE : View.GONE}"
              android:layout_width="match_parent"
              android:layout_height="wrap_content" />

          <Button
              android:id="@+id/btnClose"
              tools:text="@string/historian_close_app_button_text"
              android:text="@{viewModel.closeAppButtonText}"
              android:visibility="@{viewModel.closeActivityEnable ? View.VISIBLE : View.GONE}"
              android:layout_width="match_parent"
              android:layout_height="wrap_content" />

          <Button
              android:id="@+id/btnDetails"
              tools:text="@string/historian_details_button_text"
              android:text="@{viewModel.detailsButtonText}"
              android:visibility="@{viewModel.detailsButtonEnable ? View.VISIBLE : View.GONE}"
              android:layout_width="match_parent"
              android:layout_height="wrap_content" />

          <Button
              android:id="@+id/btnHistorian"
              android:text="@string/historian_open_button_text"
              android:layout_width="match_parent"
              android:layout_height="wrap_content" />
        </LinearLayout>
      </LinearLayout>
    </FrameLayout>

  </RelativeLayout>
</layout>
